/**
 * `@qualroteiro/fuel` — fuel consumption and cost arithmetic.
 *
 * Pure functions only. No network I/O, no HTTP client, no persistence.
 */

export type { FuelEstimate, FuelEstimateInput } from './estimate.js';
export { estimateFuel } from './estimate.js';
